---
interface Props {
  schemas: object[];
}
const { schemas } = Astro.props;
const json = JSON.stringify(schemas.length === 1 ? schemas[0] : { '@context': 'https://schema.org', '@graph': schemas });
---
<Fragment set:html={`<script type="application/ld+json">${json}</script>`} />
